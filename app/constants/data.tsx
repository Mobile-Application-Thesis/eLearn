const data = {
  role: {
    student: 'student',
    teacher: 'teacher',
  },
}

export declare type LessonDataTypes = {
  assessmentId: string
  attachments: string[]
  createdBy: string
  externalLinks: string[]
  htmlContent: string
  title: string
  id?: string
}

export default data
